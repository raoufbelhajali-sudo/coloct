"use client";

// Fond blanc partout par défaut (web et app). Le fond « grunge » sombre n'est
// appliqué QUE sur la page swipe en PC web (voir src/app/swipe/page.tsx).
export default function FondAccueil() {
  return null;
}
